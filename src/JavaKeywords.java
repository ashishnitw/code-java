// package coreJava;

public class JavaKeywords {
	public static void main(String[] args) {
		//TRANSIENT: At the time of serialization, if we don�t want to save value of a particular variable in a file, then we use transient keyword
		
		//VOLATILE: volatile keyword here makes sure that the changes made in one thread are immediately reflect in other thread
		//For Java, �volatile� tells the compiler that the value of a variable must never be cached as its value may change outside of the scope of the program itself.
		
		//strictfp is a keyword in java used for restricting floating-point calculations and ensuring same result on every platform while performing operations in the floating-point variable.
		
	}
}
